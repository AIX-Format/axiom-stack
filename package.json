{
  "name": "axiom-stack",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "clean": "turbo clean"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "packages/@axiom/*"
  ],
  "packageManager": "pnpm@10.33.4",
  "engines": {
    "node": ">=22.0.0"
  },
  "devDependencies": {
    "turbo": "^2.8.0"
  }
}
